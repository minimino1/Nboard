# Community Layouts

Drop community-submitted layout XML files in this folder via pull requests.

Naming convention:
- Use lowercase kebab-case filenames.
- Format: `<language>-<layout>-<style>.xml`
- Example: `french-azerty-gboard.xml`

Each file should follow the `layout_template.xml` structure.
